name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: Scripts/bootstrap-bundled-assets.sh
      - run: swift test
      - run: Scripts/build-app.sh
