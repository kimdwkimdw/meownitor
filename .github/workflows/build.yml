name: Build

on:
  push:
  pull_request:

jobs:
  macos:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - run: Scripts/bootstrap-bundled-assets.sh
      - run: swift test
      - run: Scripts/build-app.sh
